#INSTALLATION#
--------------
# Step 0: clone .Vim repository into ~/.vim
> git clone http://github.com/username/dotvim.git ~/.vim
# Step 1: link ~/.vim/vimrc to ~/.vimrc
> ln -s ~/.vim/vimrc ~/.vimrc
# Step 2: install plugins using git submodules
> cd ~/.vim
> git submodule init
> git submodule update
--------------

#MAINTENANCE#
-------------
# Update your plugins
> cd ~/.vim
> git submodules update
-------------

#MODIFICATION#
--------------
# Add a new plugin
> git submodule add $GITPATH bundle/$PLUGINNAME
> git add .
> git commit -m "Install $PLUGINNAME bundle as a submodule."
--------------
